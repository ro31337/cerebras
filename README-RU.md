# LiteLLM Proxy: Cerebras → Z.AI Fallback

[English version](README.md)

LiteLLM прокси-сервер с автоматическим переключением с Cerebras на Z.AI при превышении лимитов.

## Зачем это нужно?

У нас есть доступ к потрясающей модели **GLM-4.6**, и у нас есть **Cerebras** - компания, производящая AI-чипы с невероятно быстрой инференсной скоростью **1000 токенов/секунду**! Также у нас есть обычный API от Z.AI.

Вот классная штука: **это одна и та же модель** (GLM-4.6) на обеих платформах. Так почему бы не использовать сначала супер-быстрые запросы через Cerebras, а когда достигнем лимитов, **автоматически переключаться на Z.AI**? Таким образом мы получаем:

- ⚡ **Молниеносные ответы** от Cerebras (1000 токенов/сек!)
- 🔄 **Автоматическое переключение** на Z.AI при достижении лимитов
- 🚀 **Лучшую производительность** чем при использовании только Z.AI

Это как турбо-кнопка, которая автоматически переключается в обычный режим когда нужно. Лучшее из двух миров!

## Инструкция по установке

### 1. Установка uv

Сначала установите `uv` - быстрый установщик Python пакетов, написанный на Rust. Он значительно быстрее pip и легко работает с виртуальными окружениями.

```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

Если хотите узнать больше про uv, прочитайте эту статью: https://emily.space/posts/251023-uv

### 2. Создание виртуального окружения

Создайте директорию `.venv` для изолированных Python зависимостей:

```bash
uv venv
```

### 3. Активация виртуального окружения

**ВАЖНО:** Вам нужно активировать виртуальное окружение **каждый раз** перед запуском прокси или тестов. Это одна из двух основных команд, которые вы будете использовать:

```bash
source .venv/bin/activate
```

### 4. Установка зависимостей

Запустите это **один раз** после начальной настройки для установки всех необходимых пакетов:

```bash
uv sync
```

### 5. Настройка API ключей

Скопируйте пример файла окружения и добавьте ваши API ключи:

```bash
cp .env.example .env
```

Теперь отредактируйте `.env` и установите ваши API ключи:

#### Получение Cerebras API ключа (БЕСПЛАТНО с лимитами)
1. Перейдите на https://cloud.cerebras.ai/
2. Кликните на выпадающее меню **"Personal"** в верхней панели навигации
3. Выберите **"API Keys"**
4. Создайте или скопируйте ваш персональный API ключ (бесплатный тариф с ограничениями)

#### Получение Z.AI API ключа
1. Перейдите на https://z.ai/manage-apikey/apikey-list
2. Создайте или скопируйте ваш API ключ

Ваш `.env` должен выглядеть так:
```
CEREBRAS_API_KEY=csk-ваш-реальный-ключ-здесь
ZAI_API_KEY=ваш-реальный-zai-ключ-здесь
LITELLM_MASTER_KEY=sk-1234
```

## Запуск прокси

Запустите прокси-сервер (это вторая основная команда):

```bash
./start_proxy.sh
```

Вы должны увидеть вывод примерно такой:

```
Starting LiteLLM proxy server...
Cerebras (primary) → Z.AI GLM-4.6 (fallback)

INFO:     Started server process [78824]
INFO:     Waiting for application startup.

   ██╗     ██╗████████╗███████╗██╗     ██╗     ███╗   ███╗
   ██║     ██║╚══██╔══╝██╔════╝██║     ██║     ████╗ ████║
   ██║     ██║   ██║   █████╗  ██║     ██║     ██╔████╔██║
   ██║     ██║   ██║   ██╔══╝  ██║     ██║     ██║╚██╔╝██║
   ███████╗██║   ██║   ███████╗███████╗███████╗██║ ╚═╝ ██║
   ╚══════╝╚═╝   ╚═╝   ╚══════╝╚══════╝╚══════╝╚═╝     ╚═╝

LiteLLM: Proxy initialized with Config, Set models:
    glm-4.6
    zai-glm-4.6
    glm-4.5-air
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:4000 (Press CTRL+C to quit)
```

**⚠️ Примечание:** Если порт 4000 уже занят, будет выбран случайный порт. Обратите внимание на вывод, чтобы увидеть какой порт на самом деле используется!

## Тестирование через curl

Протестируйте прокси простым запросом:

```bash
curl -X POST "http://localhost:4000/chat/completions" \
  -H "Authorization: Bearer sk-1234" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "glm-4.6",
    "messages": [{"role": "user", "content": "Привет!"}],
    "max_tokens": 50
  }'
```

## Тестирование механизма fallback

Запустите 20 параллельных запросов чтобы достичь лимита Cerebras и проверить автоматическое переключение на Z.AI:

```bash
source .venv/bin/activate
python test_parallel.py
```

Ожидаемый вывод:
```
✓ Request  1: cerebras/zai-glm-4.6           (3.05s)
✓ Request  2: glm-4-plus                     (4.98s)
✓ Request  3: glm-4-plus                     (4.62s)
...
Cerebras responses: 5
Z.AI responses:     15
Errors:             0

✓✓✓ Fallback to Z.AI IS WORKING! ✓✓✓
```

## Конфигурация

### Модели
- **glm-4.6**: Cerebras (основная, 1000 токенов/с, 10 запросов/мин) → Z.AI fallback (для Opus/Sonnet)
- **glm-4.5-air**: Только Z.AI, без fallback (для Haiku - быстрые, дешевые задачи)

### Настройки прокси
- **Endpoint**: http://localhost:4000
- **API Key**: sk-1234

## Использование с Claude Code

### 1. Настройка Claude Code

Настройте Claude Code для использования этого прокси вместо Anthropic API:

```bash
./configure_claude_env.sh
```

Это настроит Claude Code на использование:
- **Opus/Sonnet** → glm-4.6 (Cerebras с Z.AI fallback)
- **Haiku** → glm-4.5-air (только Z.AI, для быстрых задач)

### 2. Запуск прокси и Claude Code

```bash
# Терминал 1: Запустите прокси
./start_proxy.sh

# Терминал 2: Запустите Claude Code
claude
```

### 3. Проверка конфигурации

После запуска Claude Code, проверьте что он использует локальный прокси, выполнив:

```
/status
```

Вы должны увидеть:
```
 Version: 2.0.47
 Auth token: ANTHROPIC_AUTH_TOKEN
 Anthropic base URL: http://localhost:4000
 Model: Default (glm-4.6)
```

**✅ Если видите `Anthropic base URL: http://localhost:4000`** - отлично! Claude Code использует ваш локальный прокси с Cerebras → Z.AI fallback.

**❌ Если видите другой URL** - запустите `./configure_claude_env.sh` снова и перезапустите Claude Code.

## Файлы

- `README.md` - английская версия документации
- `README-RU.md` - этот файл (русская версия)
- `config.yaml` - конфигурация LiteLLM с fallback
- `.env` - API ключи для Cerebras и Z.AI
- `.env.example` - шаблон переменных окружения
- `start_proxy.sh` - запуск прокси-сервера
- `configure_claude_env.sh` - настройка Claude Code для использования прокси
- `test_parallel.py` - тест механизма fallback
- `test_full.py` - полный тест ответов
